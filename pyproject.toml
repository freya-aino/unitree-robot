[project]
name = "unitree_robot"
version = "0.1.0"
description = ""
authors = [
    {name = "freya",email = "freya.aino@pm.me"}
]
readme = "README.md"
requires-python = "~3.10"
dependencies = [
    "cyclonedds==0.10.2",
    "pydantic (>=2.11.9,<3.0.0)",
    "numpy (>=1.26,<2.0)",
]

[tool.poetry.scripts]
test = "pytest:main"

[tool.poetry.group.dev.dependencies]
pandas = "^2.3.2"
pytest = "^8.4.2"
pytest-cov = "^7.0.0"
pytest-mock = "^3.15.0"
brax = "^0.13.0"
warp-lang = "^1.9.0"
scipy = ">=1.15,<2.0"
lightwheel = {url = "https://github.com/freya-aino/lightwheel/releases/download/v1.0.1/lightwheel-1.0.1-py3-none-any.whl"}
usd-core = "^25.8"


[tool.poetry.group.train.dependencies]
brax = "^0.13.0"
warp-lang = "^1.9.0"
scipy = ">=1.15,<2.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
